{
  "name": "aws-lambda",
  "version": "1.0.0",
  "description": "test env to develop AWS lambdas locally",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "start:dev": "nodemon -r dotenv/config --inspect app.js dotenv_config_path=./.env",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.321.1",
    "@aws-sdk/lib-dynamodb": "^3.321.1"
  },
  "type": "module",
  "devDependencies": {
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.22"
  }
}
